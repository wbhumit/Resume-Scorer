# ATS Resume Scorer - Environment Variables
# Copy this file to .env and update values as needed

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: Set custom upload directory
# UPLOAD_DIR=./uploads

# Optional: Set maximum file size (in bytes)
# Default: 5MB (5 * 1024 * 1024)
# MAX_FILE_SIZE=5242880

# Optional: Enable debug logging
# DEBUG=true

# Optional: CORS Configuration (if needed for different frontend domain)
# CORS_ORIGIN=http://localhost:3000

# Optional: Rate Limiting (requests per minute)
# RATE_LIMIT=60

# Note: Most settings have sensible defaults in server.js
# Only uncomment and set these if you need to override defaults
